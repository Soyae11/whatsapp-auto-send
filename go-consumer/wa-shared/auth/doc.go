package auth

// Concern: supporting (consumer API).
//
// The API key value type: generation, SHA-256 hashing, the display hint, and what a key may
// send as. No I/O — persistence is store/keys.go, issuance is cmd/wa-admin.
